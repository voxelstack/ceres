name: style
on:
    pull_request:
    push:

jobs:
    format:
        runs-on: ubuntu-latest
        steps:
            - run: corepack enable
            - uses: actions/checkout@v5
            - uses: actions/setup-node@v6
              with:
                  cache: "yarn"
            - name: Run prettier
              run: |
                  yarn
                  yarn run format
    lint:
        runs-on: ubuntu-latest
        steps:
            - run: corepack enable
            - uses: actions/checkout@v5
            - uses: actions/setup-node@v6
              with:
                  cache: "yarn"
            - name: Run eslint
              run: |
                  yarn
                  yarn run lint
